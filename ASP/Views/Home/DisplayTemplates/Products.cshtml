@model List<ASP.Models.Home.Product>

<div style="display:flex; flex-wrap: wrap; gap: 10px">
    @for (int i = 0; i < Model.Count; i++)
    {
        @Html.DisplayFor(m => m[i])
        ;
    }
</div>